import React, {FC} from 'react';
import Footer from './Footer';

const footerStory = {
    title: 'Components/Footer',
    component: Footer,
};

export default footerStory;

export const Default: FC = () => <Footer/>;